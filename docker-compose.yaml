services:
  postgres:
    image: docker.io/library/postgres:18.6
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin123
      POSTGRES_DB: thoughts
    ports:
      - "5432:5432"
    volumes:
      - thoughts_postgres_data:/var/lib/postgresql

volumes:
  thoughts_postgres_data:
